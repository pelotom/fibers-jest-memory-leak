{
  "name": "sqlite3-jest-memory-leak",
  "version": "1.0.0",
  "scripts": {
    "test": "node --expose-gc ./node_modules/.bin/jest --no-cache --runInBand --watchAll --logHeapUsage --detectLeaks"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jest": "^26.0.1",
    "sqlite3": "^4.2.0",
    "weak-napi": "^2.0.2"
  }
}
