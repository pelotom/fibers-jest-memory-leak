{
  "name": "fibers-jest-memory-leak",
  "version": "1.0.0",
  "scripts": {
    "test": "node -r fibers --expose-gc ./node_modules/.bin/jest --runInBand --watchAll --logHeapUsage"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fibers": "^5.0.0",
    "jest": "^26.0.1",
    "weak-napi": "^2.0.2"
  }
}
